name: resgate_client
description: A client for Resgate.
version: 1.0.0

environment:
  sdk: ^3.4.3

# Add regular dependencies here.
dependencies:
  web_socket_channel: ^3.0.0

dev_dependencies:
  build_runner: ^2.4.14
  lints: ^5.1.1
  mockito: ^5.4.5
  test: ^1.24.0
